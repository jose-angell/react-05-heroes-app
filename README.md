# Heroes App

## Levantar desarrollo

1. Clonar el repositorio
2. Editar el archivo `.env` con las variables de enteorno basado en el archivo `.env.template`
3. Ejecutar `npm install`
4. Ejecutar `npm run dev`
5. En la carpeta del proyecto de backend ejecutar `npm run start:dev`